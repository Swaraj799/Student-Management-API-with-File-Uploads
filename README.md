# Student-Management-API-with-File-Uploads
A Node.js and Express API to manage student data with support for profile picture uploads. Uses MongoDB for storage and includes robust error handling for both file uploads and general API operations.
